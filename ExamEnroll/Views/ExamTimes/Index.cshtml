@model IEnumerable<ExamEnroll.ExamTime>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>考试时间</h2>

<p>
    @Html.ActionLink("新增", "Create")
</p>
<table class="table">
    <tr>
        <th>
            时间
        </th>        
        <th>
           操作
        </th>        
    </tr>  

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>     
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Time }) |
            @Html.ActionLink("Details", "Details", new { id=item.Time }) |*@
            @Html.ActionLink("删除", "Delete", new { id=item.Time })
        </td>
    </tr>
}

</table>
